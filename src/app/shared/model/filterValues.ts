export interface FilterValues {
    keyactivity: string| null,
    idactivity: number| null,
    keyservice: string | null,
    idservice: number | null,
    keystaff: string | null,
    keyshift: string | null,
    service: string | null,
    idduration: number| null,
    staff: string | null,
    startDate: string | null,
    endDate: string | null,
    shift: string | null,
    name?: string | null
}